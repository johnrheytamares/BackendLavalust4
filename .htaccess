# .htaccess for LavaLust on Render (Native PHP/Apache)
# Rewrite all requests to public/index.php (front controller pattern)

RewriteEngine On
RewriteBase /

# Skip real files/folders (e.g., /uploads/, images)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else to public/index.php
RewriteRule ^(.*)$ public/index.php [QSA,L]

# Optional: Force HTTPS (Render already does this, but good practice)
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]